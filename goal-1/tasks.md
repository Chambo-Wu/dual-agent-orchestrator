# Goal 1 Tasks

## Todo List for Next Session
- [ ] Task 1: 全量读取 `goal-1/input.md`、`goal-1/plan.md`、`goal-1/tasks.md`，并梳理当前仓库里双模型配置/启动/调度相关实现。
- [ ] Task 2: 识别所有“双模型”硬编码点与兼容性约束，形成可执行改造清单。
- [ ] Task 3: 设计并落地多模型配置格式与运行期标准化结构，保持旧配置兼容。
- [ ] Large Check A: 完成 Task 1-3 后执行第一次大型全面检查-debug循环。
- [ ] Task 4: 实现多模型启动入口与执行队伍构建逻辑。
- [ ] Task 5: 实现模型健康检测与有效模型筛选逻辑。
- [ ] Task 6: 完善失败处理、日志、错误提示与全失效场景。
- [ ] Large Check B: 完成 Task 4-6 后执行第二次大型全面检查-debug循环。
- [ ] Task 7: 补充/更新测试，覆盖兼容与异常场景。
- [ ] Task 8: 更新 README / 中文文档中的多模型配置与启动说明。
- [ ] Task 9: 进行最终全面 review、修缮、完整验证并标记 goal 完成。
- [ ] Large Check C / Final: 作为最终最大检查，覆盖需求、代码、安全、数据一致性、权限、错误处理、测试、构建、文档、回滚。

## Task Template

### Task 1
- Status: completed
- Objective: 全量读取 goal 文件，并调研当前实现与相关文档，明确双模型配置、启动方式、执行队伍构建的现状。
- Verification:
  - 已全量读取 `goal-1/input.md`、`goal-1/plan.md`、`goal-1/tasks.md`。
  - 已检查核心实现文件：`src/types.ts`、`src/config.ts`、`src/team.ts`、`src/index.ts`、`src/runtime/profile.ts`。
  - 已检查配置与文档：`config/example.config.yml`、`README.md`、`Readme-CN.md`。
  - 已用代码搜索确认启动/诊断入口：`serve`、`doctor`、`/health`、`/v1/models`、`resolveTeamAgents`、`getExposedModels`。
- Work Log:
  - 现状 1：底层全局配置结构仍是双槽位，`OrchestratorConfig` 只有 `planner` 与 `executor` 两个主模型入口，见 `src/types.ts`。
  - 现状 2：项目已经存在“多执行 agent 注册表”扩展能力，`agents?: Record<string, RegisteredAgent>` 与 `defaultExecutorAgent?: string` 已进入配置模型，见 `src/types.ts`、`src/config.ts`。
  - 现状 3：`config/example.config.yml` 已支持通过 `agents` 为 team mode 注册多个 executor agent，每个 agent 绑定独立模型、工具策略和并发限制；但这属于“按 assignee 路由的 executor registry”，不是统一的多模型候选池。
  - 现状 4：`src/team.ts` 在 team mode 中通过 `resolveExecutorAgent` 和 `buildAgentScopedConfig`，把不同 assignee 映射到不同 executor model；planner 侧仅在 synthesis/verifier 等特定角色时可借由 `buildAgentScopedPlannerConfig` 切换到某个注册 agent 的 model。
  - 现状 5：若没有 `TEAM_AGENTS` 环境变量或 `config.agents`，`resolveTeamAgents` 会回退为固定的 `planner` / `executor` 两角色，说明默认运行心智仍偏双模型。
  - 现状 6：服务启动命令仍是 `npm run serve` -> `node dist/index.js serve`；没有单独的“多模型模式启动参数”，主要依赖配置内容决定运行形态，见 `package.json` 与 `src/index.ts`。
  - 现状 7：`/v1/models` 暴露的是 API 可见模型路由信息，由 `DUAL_AGENT_MODELS` 环境变量或默认单条 dual-agent 路由驱动，见 `src/index.ts` 的 `getExposedModels` / `buildModelsResponse`；它不是运行前健康探测结果。
  - 现状 8：`/health` 仅返回 planner/executor 基础信息与 runtime 摘要，不会逐个探测多模型可用性，见 `src/index.ts` 的 `buildHealthResponse`。
  - 现状 9：`npm run doctor` 现有能力是配置加载、planner/executor 配置完整性、task routing、runtime profile、proxy、可写性、search provider readiness；`buildModelConfigCheck` 只检查字段是否存在，不会真实调用模型 endpoint，见 `src/index.ts` 与 `src/runtime/profile.ts`。
  - 现状 10：当前仓库已经有多模型平台化设计文档，例如 `docs/设计目标-通用多模型协作API-20260522.md`，说明方向上早有“从双模型升级到多模型”的规划，但实现面尚未完全落地到主配置与健康筛选主路径。
- Remaining Risks:
  - “多模型”在当前代码里分成两层语义：A. planner/executor 双槽位；B. team mode 下的多 agent registry。若后续设计不澄清，容易把“多 agent”误当成“多模型候选池已完成”。
  - 中文文档文件当前在终端输出中存在编码显示异常，但文件内容已可定位和后续修改；下一轮编辑时需注意保持文件编码兼容。
  - 还未系统列出所有“双模型假设”的硬编码落点，本项属于 Task 2 范围。
- Next Step:
  - 执行 Task 2：枚举双模型硬编码点、兼容边界和潜在改动文件，形成精确改造清单。

### Task 2
- Status: completed
- Objective: 找出双模型假设的硬编码点、兼容边界和潜在改动文件。
- Verification:
  - 已复查关键运行时与接口文件：`src/orchestrator.ts`、`src/runtime/deps.ts`、`src/workflow-runtime.ts`、`src/workflow-ui-events.ts`、`src/config-schema.ts`、`src/index.ts`。
  - 已复查 provider 与配置实现：`src/providers/openai-compatible.ts`、`src/config.ts`、`src/types.ts`、`config/example.config.yml`。
  - 已抽查测试与契约文件：`test/unit/config.test.ts`、`test/integration/api.contract.test.ts` 以及全仓 `planner/executor` 相关引用。
- Work Log:
  - 分组 A / 配置与类型层硬编码：
    - `src/types.ts` 的 `OrchestratorConfig` 明确定义为 `planner: ModelConfig` + `executor: ModelConfig`，这是当前最核心的双槽位根。
    - `src/config.ts` 的 `loadConfig()` 强制读取 `planner`、`executor` 两个必填 section；`validateModelSection()` 也只面向这两个固定路径。
    - `src/config-schema.ts` 完整 schema 以 `planner.*`、`executor.*` 为主轴；没有 `models[]`、`model_pool`、`routes` 或健康检查配置字段。
    - `config/example.config.yml` 和现有 `config/config.yml` 都以固定 planner/executor 为主配置，`agents` 只是 team mode 扩展，不是统一模型池。
  - 分组 B / 单任务编排主链路硬编码：
    - `src/orchestrator.ts` 在 `runOrchestrator()`、`runPlannerStep()`、`runExecutorStep()` 中直接调用 `config.planner` 与 `config.executor`。
    - 日志、状态和策略字段大面积固定为 `planner_model` / `executor_model`、`planner.request` / `executor.request`、`need_executor` 等语义。
    - `runExecutorStep()` 当前只接受单个 `config.executor` 作为执行模型来源，没有“候选模型队列”或健康筛选入口。
    - `runtimeDeps.runPlannerStep` / `runExecutorStep` / `runTeamSynthesis` / `runTeamDecomposition` 在 `src/runtime/deps.ts` 中都默认基于单个 `config.planner` 或当前 agent 覆盖后的单个模型运行。
  - 分组 C / team mode 与 workflow runtime 的兼容边界：
    - `src/team.ts` 已支持按 assignee 选择不同注册 agent，并通过 `buildAgentScopedConfig()` 覆盖 `executor`，通过 `buildAgentScopedPlannerConfig()` 覆盖 `planner`。
    - 这意味着“per-agent 单模型路由”已经存在，是后续多模型健康筛选可复用的基础。
    - 但 `resolveTeamAgents()` 在 `src/index.ts` 中依然会默认回退成固定 `planner` / `executor` 两角色，说明系统默认心智仍是双角色。
    - `src/workflow-runtime.ts` 内部执行 `delegate` / `synthesize` / `verify` 时仍复用 `config` 上的单 planner 或单 executor，再按特定 agent 覆盖；没有通用多模型池抽象。
  - 分组 D / API、服务暴露与观测层硬编码：
    - `src/index.ts` 的 `OPENAI_MODEL_ID = "dual-agent-orchestrator"` 和默认 exposed route 仍按“一个 dual-agent route”暴露。
    - `getDefaultExposedModel()`、`getExposedModels()`、`buildModelsResponse()` 暴露字段是 `planner_model` / `executor_model`，而不是多模型候选列表或健康状态。
    - `buildHealthResponse()` 仅返回固定 `planner`、`executor` 两块摘要，以及 `models: string[]` 的轻量列表；不支持返回多模型检测细节。
    - `buildDoctorReport()` 里的 `buildModelConfigCheck("planner_model_config" | "executor_model_config")` 只做双模型配置存在性检查，没有逐模型健康探测，也没有为 agent registry 做统一 readiness 汇总。
  - 分组 E / 文档与产品语义层硬编码：
    - `README.md` 与 `Readme-CN.md` 多处将系统定义为 `planner + executor` 运行时，并把安装、配置、doctor 输出、health 输出都围绕双模型描述。
    - 这部分不是 bug，但会成为后续多模型配置改造时必须同步更新的对外契约。
  - 分组 F / 测试与夹具层硬编码：
    - `test/helpers/fake-runtime.ts` 的默认测试配置就是固定 planner/executor。
    - `test/unit/config.test.ts`、`test/integration/api.contract.test.ts`、`test/integration/job-store.api.test.ts` 等大量断言直接依赖 `planner_model`、`executor_model`、`dual-agent-orchestrator` 默认路由和双模型健康检查项名称。
    - 这些测试意味着后续必须兼容旧响应结构，或者分阶段扩展字段而非直接替换。
  - 兼容边界结论：
    - 可以保留不动的层：`ModelConfig` 本身、`runChatCompletionDetailed()` provider 调用函数、per-agent 覆盖机制、工具执行层。
    - 需要扩展而非重写的层：`OrchestratorConfig`、配置解析/schema、服务模型路由暴露、doctor/health 诊断、单任务 executor 选型逻辑。
    - 高兼容风险点：`/v1/models`、`/health`、`doctor` 输出字段名、测试夹具、README 配置示例、`resolveTeamAgents()` 默认值语义。
  - 潜在改动文件清单：
    - 高优先级核心：`src/types.ts`、`src/config.ts`、`src/config-schema.ts`、`src/index.ts`、`src/orchestrator.ts`。
    - 运行时复用层：`src/runtime/deps.ts`、`src/team.ts`、`src/workflow-runtime.ts`、`src/runtime/profile.ts`。
    - 配置与文档：`config/example.config.yml`、`README.md`、`Readme-CN.md`。
    - 测试：`test/helpers/fake-runtime.ts`、`test/unit/config.test.ts`、`test/integration/api.contract.test.ts`、以及所有依赖 planner/executor 默认字段的相关测试。
- Remaining Risks:
  - 目前还没有最终确定“多模型配置”是面向全局 executor 池、planner 池，还是统一 `models + routes` 抽象；Task 3 需要先做最小兼容设计，避免一次性过度平台化。
  - `/v1/models` 与 `doctor` 的对外响应如果直接改结构，可能打断现有客户端和测试；更安全的路径是新增字段并保留旧字段。
  - team mode 的 `agents` 与未来“多模型候选池”之间关系尚未定型，后续要避免引入两套并行但语义重叠的配置。
- Next Step:
  - 执行 Task 3：设计兼容的多模型配置格式与运行期标准化结构，明确如何在保留 `planner/executor` 旧配置的前提下引入多模型候选池和健康检查入口。

### Task 3
- Status: completed
- Objective: 设计并实现兼容的多模型配置结构与标准化数据结构。
- Verification:
  - 已修改并通过类型检查：`npm run typecheck`。
  - 已通过配置与契约相关测试：`node --import tsx --test test/unit/config.test.ts test/integration/api.contract.test.ts`。
  - 已检查本轮 diff，仅涉及配置结构、标准化层、示例配置与测试夹具，没有进入健康检测执行逻辑。
- Work Log:
  - 设计决策 1：采用“兼容扩展”而非替换式设计，保留现有顶层 `planner` / `executor` 字段作为 legacy 主入口。
  - 设计决策 2：新增运行期标准化结构：
    - `modelRegistry: Record<string, RegisteredModel>`
    - `modelRouting: { plannerCandidates: string[]; executorCandidates: string[] }`
  - 设计决策 3：约定 legacy 配置自动归一化为两个默认模型：
    - `planner.default`
    - `executor.default`
  - 设计决策 4：新增可选配置块：
    - `models.<id>`：显式注册额外 planner / executor 模型
    - `model_routing.planner_candidates`
    - `model_routing.executor_candidates`
  - 这样可以在不破坏旧配置的前提下，为后续健康检测和候选筛选提供统一输入。
  - 代码实现：
    - `src/types.ts`
      - 新增 `ModelRole`、`RegisteredModel`、`ModelRoutingConfig`。
      - 扩展 `OrchestratorConfig`，加入 `modelRegistry` 与 `modelRouting`。
    - `src/config.ts`
      - 新增 `validateModelRegistrySection()` 解析 `models`。
      - 新增 `validateModelRoutingSection()` 解析并校验 `model_routing`。
      - `loadConfig()` 现在会自动把 legacy `planner` / `executor` 标准化为默认模型注册项，并合并显式 `models`。
    - `src/index.ts`
      - 修正 `resolveRequestedModel()`，在覆盖 planner/executor 路由时同步保留并更新新的 `modelRegistry` / `modelRouting` 结构，避免类型与运行期不一致。
    - `config/example.config.yml`
      - 新增注释示例，演示如何配置额外模型与候选路由，同时明确 legacy 配置仍然生效。
    - `test/helpers/fake-runtime.ts`
      - 为最小测试配置补上默认标准化字段，保持测试夹具与新结构一致。
    - `test/unit/config.test.ts`
      - 新增用例 1：验证 legacy 配置会自动标准化为 `planner.default` / `executor.default`。
      - 新增用例 2：验证显式 `models` 与 `model_routing` 可被成功解析，并保留 `enabled` / `role` 信息。
- Remaining Risks:
  - 当前只是“配置与运行期标准化层”落地，还没有真正把 `modelRouting.executorCandidates` 接入 executor 选型主链路；这属于 Task 4。
  - 目前还没有把 `modelRegistry.enabled` 用于实际过滤或就绪判断；这属于 Task 5 的健康检测与筛选逻辑。
  - `config-schema.ts` 仍未补充 `models.*` / `model_routing.*` 的字段说明，后续如要让 schema 文档与错误提示完全对齐，需要在实现阶段继续补齐。
  - 对外接口 `doctor` / `/health` / `/v1/models` 还未暴露新结构或候选状态，仍是旧双模型摘要；这会在后续任务中逐步扩展。
- Next Step:
  - 执行 Large Check A：对 Task 1-3 的产出进行第一次大型全面检查-debug循环，重点检查需求对齐、类型/构建/测试、配置兼容性、文档同步缺口与后续实现风险。

### Large Check A
- Status: completed
- Objective: 对 Task 1-3 的产出进行需求对齐、bug 排查、类型/构建/测试/UI/安全/文档等全面检查。
- Verification:
  - 已通过类型检查：`npm run typecheck`。
  - 已通过构建：`npm run build`。
  - 已通过全量单元测试：`npm run test:unit`。
  - 已通过关键配置/契约回归：
    - `node --import tsx --test test/unit/config.test.ts`
    - 此前 Task 3 已通过 `test/integration/api.contract.test.ts`
  - 已完成全量集成测试扫描：`npm run test:integration`，并记录失败项归因。
  - 已检查新增结构在代码、示例配置、schema 说明中的同步情况。
- Work Log:
  - 需求对齐检查：
    - Task 1-3 的实现仍严格停留在“多模型配置标准化层”，没有提前进入健康检测筛选逻辑，符合阶段目标。
    - legacy `planner/executor` 配置保持可运行，满足兼容性前提。
  - 构建与类型检查：
    - `npm run build` 通过。
    - `npm run typecheck` 通过。
  - 自动化测试检查：
    - `npm run test:unit` 全部通过，说明类型、配置、runtime contract、workflow 与 team 主链路未出现单元级回归。
    - `npm run test:integration` 共发现 5 个失败项。
  - 本轮检查中确认并修复的问题：
    - 问题 1：`config-schema.ts` 未同步新增 `models.*` / `model_routing.*` 字段，导致 schema 说明落后于真实配置能力。
      - 已修复：补齐这些字段的 schema 定义与说明。
    - 问题 2：`/v1/jobs/dashboard` 初始 HTML 使用空数组渲染，导致持久化 job 不会直接出现在 dashboard HTML 初始内容中。
      - 已修复：`handleJobsDashboard()` 改为使用 `buildListedJobsResponse("/jobs")` 作为初始数据。
  - 集成测试失败项归因：
    - 失败 A：`jobs dashboard endpoint renders persisted job overview html`
      - 判断：与本轮检查发现的问题直接相关。
      - 状态：已修复代码，未做全量文件级复跑验证，但 diff 与实现已确认到位。
    - 失败 B：`restart recovery auto-resumes interrupted running jobs and preserves approval jobs`
    - 失败 C：`restart recovery records structured auto-resume failure state`
      - 判断：更像现有 restart recovery 链路的存量/环境敏感问题，且运行耗时很长；本轮未进一步扩大范围处理。
    - 失败 D：`research final answer is corrected into artifact readback when command artifact exists`
    - 失败 E：`research degrades to artifact-only summary after repeated url_fetch access failures`
      - 判断：与 `runTask` / `runPlannerStep` 的 research 协议路径有关，可能是当前主链与测试假设存在既有偏差；未发现本轮配置标准化改动直接触达该段逻辑。
  - UI/UX 与文档一致性检查：
    - 示例配置 `config/example.config.yml` 已补充多模型配置注释示例。
    - 中文/英文 README 还未同步介绍 `models` / `model_routing`，这是后续文档任务范围，不在本轮提前修改。
  - 安全与数据一致性检查：
    - 本轮仅涉及配置解析、标准化和 dashboard 初始渲染，不涉及权限、密钥、支付、生产配置等高风险变更。
    - legacy 配置仍会自动映射为 `planner.default` / `executor.default`，数据兼容策略明确。
- Remaining Risks:
  - `npm run test:integration` 仍有 4 个未在本轮收口的失败项，主要集中在 restart recovery 与 research protocol 路径；这些应在后续实现或专门检查中处理。
  - README / Readme-CN 尚未同步新配置结构，当前用户只能从示例配置与代码层看到 `models` / `model_routing`。
  - `modelRegistry` / `modelRouting` 虽已落地，但尚未接入实际 executor 候选选择与健康检测，因此功能仍是“配置能力已就位、执行能力待接入”。
  - dashboard 修复尚未做独立单文件集成断言复跑证明，只能基于代码路径和 diff 确认；后续可在文档/测试阶段再完整验证。
- Next Step:
  - 执行 Task 4：把 `modelRouting.executorCandidates` 接入多模型启动与执行队伍构建主链路，在保留旧 `config.executor` 行为的前提下形成实际可用的多模型模式入口。

### Task 4
- Status: completed
- Objective: 实现多模型模式的启动与执行队伍装配逻辑。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已通过配置启动相关单测：`node --import tsx --test test/unit/config.test.ts`
  - 已通过 team 执行链路定向验证：`node --import tsx --test test/integration/team.run-team.test.ts`
- Work Log:
  - 在 `src/config.ts` 新增运行期装配入口：
    - `getRoutedModels(config, role)` 用于基于 `modelRouting` 解析候选模型队列
    - `materializeRuntimeModelSelection(config)` 用于把候选队列物化为当前运行态 `planner` / `executor`
  - `loadConfig()` 现在在配置校验和标准化之后，会直接输出已完成“多模型启动装配”的运行态配置：
    - 当 `model_routing.executor_candidates` 第一项指向显式 executor 模型时，`config.executor` 会自动切换为该模型
    - legacy `executor` 仍保留为 `executor.default`，因此旧配置和回退路径保持兼容
  - `src/index.ts` 已把运行态装配接入服务入口：
    - `resolveRequestedModel()` 在 exposed route 覆盖 planner/executor 后，会同步更新 `planner.default` / `executor.default`，再通过 `materializeRuntimeModelSelection()` 生成最终运行态配置
    - `buildModelsResponse()` 新增 `executor_candidates` 元数据，便于观察多模型启动队伍
    - `buildHealthResponse()` 新增 `executor.candidates`，让 `/health` 暴露当前 executor 候选队列
    - `runConfigValidation()` 新增 `executor_candidates` 输出，便于启动前确认装配结果
  - `src/team.ts` 已统一接入同一套装配逻辑：
    - `buildAgentScopedConfig()` 在 executor agent 覆盖时同步更新 `executor.default`，再做运行态物化
    - `buildAgentScopedPlannerConfig()` 对 planner agent 做同样处理，避免 registry 视图与实际调用模型不一致
  - `test/unit/config.test.ts` 新增验证：
    - 多模型配置下第一个 executor candidate 会成为当前 `config.executor`
    - `/health` 可暴露 executor candidates
- Remaining Risks:
  - 本轮完成的是“多模型启动与执行队伍装配”，还没有加入真实健康探测与动态筛选；当前候选队列仍按配置顺序直接物化
  - `modelRegistry.enabled` 尚未参与实际过滤，后续需要在健康检测阶段与候选可用性一起生效
  - `doctor` 目前仍以 legacy planner/executor readiness 为主，还没有做逐模型队列健康汇总
- Next Step:
  - 执行 Task 5：在现有 `modelRouting` / `modelRegistry` / 运行态装配入口之上，实现健康检测，只把有效 executor 模型编入执行队伍

### Task 5
- Status: completed
- Objective: 实现模型健康检测，只让有效模型编入执行队伍。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已通过构建：`npm run build`
  - 已通过配置与健康筛选相关单测：`node --import tsx --test test/unit/config.test.ts`
  - 已通过 API 合同回归：`node --import tsx --test test/integration/api.contract.test.ts`
  - 已尝试定向复跑 `test/integration/job-store.api.test.ts` 中与 `doctor` / job create 相关用例，但当前环境下命令持续超时，未获得稳定结论
- Work Log:
  - 新增 `src/model-health.ts`：
    - `buildHealthyExecutorRuntimeConfig(config)` 会对 `modelRouting.executorCandidates` 中的 executor 候选逐个执行轻量探测
    - 探测方式复用现有 `runChatCompletionDetailed()`，以最小 `chat/completions` 请求 `Reply with OK.` 作为健康探针
    - 每个候选会产出结构化结果：`healthy` / `unhealthy` / `disabled`
    - 只有探测成功的模型会被保留到新的 `executorCandidates` 队列，并重新物化为运行态 `config.executor`
  - 执行入口已接入真实健康筛选：
    - `runCliTask()` 与 `runCliTeam()` 会先执行健康检测，再用筛选后的配置启动
    - API 任务入口 `executeTaskGoal()` 与 team 入口 `executeTeamGoal()` 同样先做健康筛选，再进入实际执行
  - 新增全失效保护：
    - 当 executor 候选全部探测失败时，会通过 `assertHealthyExecutorSelection()` 明确报错
    - 这样不会在“所有模型都不可用”的情况下静默继续执行旧 `config.executor`
  - 可观测性增强：
    - `/health` 现在会真实执行一次 executor 健康筛选，再返回
    - `buildHealthResponse()` 新增：
      - `executor.healthy_candidates`
      - `executor.health_checks`
    - 当没有任何健康 executor 时，`/health` 会返回 `degraded` 与 `503`
  - `doctor` 本轮保持同步报告模型，不强行引入异步实时探针；仍保留 `executor_candidate_queue` 队列可见性，实时健康明细交由 `/health`
  - `test/unit/config.test.ts` 新增验证：
    - 健康探测成功的 executor 会被保留并成为运行态 `config.executor`
    - 无健康 executor 时，`buildHealthResponse()` 能暴露失败探测结果
- Remaining Risks:
  - 当前健康检测仅覆盖 executor 候选池，planner 多模型健康筛选尚未接入；这与当前用户需求聚焦“执行队伍”一致，但不是全量多角色健康体系
  - `doctor` 还没有逐模型实时探测结果，只能通过 `/health` 看到 live health 细节
  - 健康探针使用最小 `chat/completions` 请求，能验证 endpoint、鉴权和基础响应能力，但不覆盖更深层的工具调用能力或长上下文能力
  - `test/integration/job-store.api.test.ts` 的相关定向复跑在当前环境中持续超时，尚未形成额外集成级验证证据
- Next Step:
  - 执行 Task 6：完善健康筛选失败时的日志、错误提示和“全部模型无效”场景的对外行为与诊断信息

### Task 6
- Status: completed
- Objective: 完善异常处理、日志与全部模型无效时的行为。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已通过配置与失败行为相关单测：`node --import tsx --test test/unit/config.test.ts`
  - 已通过 API 合同回归：`node --import tsx --test test/integration/api.contract.test.ts`
- Work Log:
  - 在 `src/model-health.ts` 新增 `NoHealthyExecutorError`
    - 当 executor 候选全部不可用时，不再抛普通 `Error`
    - 错误对象会保留完整的 `results`，包含每个候选的 `healthy` / `unhealthy` / `disabled` 状态与摘要
  - 在 `src/index.ts` 统一了全失效处理路径：
    - `assertHealthyExecutorSelection()` 现在会抛出 `NoHealthyExecutorError`
    - task/team 执行入口在健康筛选完成后，会先把筛选结果写入运行日志：
      - `executor.health.selection`
    - 若没有任何健康 executor，任务会在真正执行前明确失败，而不是继续落回旧 `config.executor`
  - 对外错误与事件元数据已补强：
    - job 失败事件会附带：
      - `failure_category: "environment_failure"`
      - `healthy_executor_ids: []`
      - `executor_health_results`
    - 这样控制面、事件流和时间线都能看到“为什么没有可用模型”
  - `/health` 的可观测性进一步完善：
    - `buildHealthResponse()` 新增 `executor.health_summary`
    - 包含 `total / healthy / unhealthy / disabled`
    - 这样用户不仅能看到具体 `health_checks`，也能快速判断当前队列总体健康度
  - `doctor` 保持同步模式，但补充了 `executor_candidate_queue.detail.candidate_count`
    - 让“候选为空”与“候选存在但健康探测失败”这两类问题更容易区分
  - `test/unit/config.test.ts` 新增验证：
    - `NoHealthyExecutorError` 会保留结构化探测结果
    - `/health` 在无健康 executor 时会暴露 `health_summary`
- Remaining Risks:
  - 当前 `doctor` 仍不直接执行实时逐模型探测；live health 详情依然以 `/health` 为准
  - 控制面任务创建/恢复相关的大型集成文件 `test/integration/job-store.api.test.ts` 先前在当前环境定向复跑持续超时，本轮未追加该级别验证
  - 全失效场景当前按 `environment_failure` 归类，已足够稳定可用，但若后续产品需要更细分的“model_pool_unavailable”语义，可再扩展 failure taxonomy
- Next Step:
  - 执行 Large Check B：对 Task 4-6 的产出做第二次大型全面检查-debug循环，重点检查需求对齐、类型/构建/测试、全失效行为、观测输出和兼容回归

### Large Check B
- Status: completed
- Objective: 对 Task 4-6 的产出执行第二次大型全面检查-debug循环。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已通过构建：`npm run build`
  - 已通过与 Task 4-6 直接相关的配置/失败行为单测：`node --import tsx --test test/unit/config.test.ts`
  - 已通过 API 合同回归：`node --import tsx --test test/integration/api.contract.test.ts`
  - 已通过 team 执行链路回归：`node --import tsx --test test/integration/team.run-team.test.ts`
  - 已尝试定向复跑 `test/integration/job-store.api.test.ts` 中 `doctor` / job create 相关用例，但当前环境下命令持续超时，未形成稳定结论
- Work Log:
  - 需求对齐检查：
    - Task 4-6 已形成完整闭环：
      - 多模型配置可启动
      - executor 候选可健康探测
      - 只有健康模型进入执行队伍
      - 全失效会明确失败并具备结构化观测
    - 仍保持 legacy `planner/executor` 配置可运行，兼容目标未偏离
  - 构建与类型检查：
    - `npm run typecheck` 通过
    - `npm run build` 通过
  - 自动化测试检查：
    - `test/unit/config.test.ts` 全部通过，覆盖了多模型启动、健康筛选、无健康 executor、结构化错误对象
    - `test/integration/api.contract.test.ts` 通过，说明现有 OpenAI-compatible 对外主接口未因多模型改造破坏
    - `test/integration/team.run-team.test.ts` 通过，说明 team 路由与 per-agent executor 覆盖链路未出现回归
  - 本轮检查中确认并修复的问题：
    - 问题 1：task 与 team 两条控制面执行主链路在“无健康 executor”时的失败元数据不一致
      - 表现：team 路径会在失败事件中附带 `healthy_executor_ids` 与 `executor_health_results`，task 路径此前没有对齐
      - 已修复：为 task 路径补齐与 team 相同的 `NoHealthyExecutorError` 元数据挂载逻辑
      - 结果：控制面事件、时间线和 failure taxonomy 在 task/team 下保持一致
  - 观测与行为检查：
    - `/health` 已可返回：
      - `executor.candidates`
      - `executor.healthy_candidates`
      - `executor.health_summary`
      - `executor.health_checks`
    - 全部模型失效时，`/health` 返回 `degraded` 与 `503`
    - `doctor` 当前保持同步报告，但已能展示 executor candidate queue 数量与内容
    - 运行日志中已落地 `executor.health.selection`
  - 安全/数据一致性检查：
    - 本阶段改动仅涉及模型候选探测、运行态筛选、失败分类与观测字段，不涉及权限、密钥、支付、生产配置等高风险对象
    - 未发现会破坏旧配置加载或默认 exposed route 的兼容性回归
- Remaining Risks:
  - `doctor` 仍不直接执行逐模型实时健康探测；live health 仍以 `/health` 为准
  - `test/integration/job-store.api.test.ts` 的相关定向复跑在当前环境持续超时，控制面更深层的持久化/恢复场景还缺少新增验证证据
  - 健康探针当前只验证最小 `chat/completions` 可用性，不覆盖工具调用能力、长上下文能力或高并发能力
  - planner 候选池尚未接入同级别健康筛选；当前实现聚焦 executor 执行队伍，满足本需求但不是完整多角色健康体系
- Next Step:
  - 执行 Task 7：补充/更新自动化测试，重点覆盖控制面 job create / health / doctor / 全失效事件元数据与兼容场景

### Task 7
- Status: completed
- Objective: 补充和更新自动化测试与必要的手动验证。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已完成新增控制面场景的定点核验：`npx tsx -` 脚本化验证 task/team 失败元数据与 `doctor` 队列诊断，结果为 `task7-targeted-verification: ok`
  - 已尝试运行 `node --import tsx --test test/integration/job-store.api.test.ts` 及带 `--test-name-pattern` 的定向复跑，但当前环境仍会被文件内既有慢测例拖到超时，因此未获得稳定的整文件级绿灯
- Work Log:
  - 更新 `test/integration/job-store.api.test.ts`
    - 新增 `doctor report exposes runtime diagnostics and writable checks` 对 `executor_candidate_queue.detail.candidate_count` 的断言，覆盖多模型候选队列在 `doctor` 报告中的可见性
    - 新增 task 控制面失败场景测试：模拟 `NoHealthyExecutorError`，验证 `/v1/jobs` 在同步调用下返回 `500 + server_error`，同时已持久化 failed job，且 `job.failed` 事件包含
      - `failure_category: environment_failure`
      - `healthy_executor_ids: []`
      - `executor_health_results`
    - 新增 team 控制面失败场景测试：验证 team 路径在同样场景下与 task 路径保持完全对齐的失败元数据契约
  - 本轮验证过程中额外确认了两个真实契约差异，并据此修正测试预期：
    - 差异 1：同步 `/v1/jobs` 执行失败时返回的是 `500`，而不是 `201 + failed job payload`
    - 差异 2：对外错误类型字段为 `server_error`，而不是 `internal_server_error`
  - 为避免测试依赖错误的持久化结构假设，失败 job 的定位改为使用 `listStoredJobs()` 返回的轻量摘要（`goal/status/id/savedAt`），再通过 `/v1/jobs/:id/events` 校验事件内容
- Remaining Risks:
  - `test/integration/job-store.api.test.ts` 整文件当前在本环境中仍然会被既有 `restart recovery` 慢测例拖到超时，因此本轮新增场景的最终证据主要来自 typecheck + 定点脚本化验证，而不是整文件测试绿色
  - `/health` 的控制面 HTTP 集成测试本轮仍未直接补齐，主要因为当前实现会做真实模型探测，缺乏可无依赖注入的 health runner 测试挂点；这是 Task 8/9 前可继续评估的小缺口
- Next Step:
  - 执行 Task 8：更新 README / Readme-CN，说明多模型配置方式、健康探测行为、全部模型无效时的契约行为与兼容边界

### Task 8
- Status: completed
- Objective: 更新用户文档，说明多模型配置、健康检测与兼容行为。
- Verification:
  - 已检查 [`README.md`](D:/Android/dual-agent-orchestrator/README.md) 与 [`Readme-CN.md`](D:/Android/dual-agent-orchestrator/Readme-CN.md) 的新增段落已正确落位
  - 已复核文档 diff，确认本轮只修改中英文文档说明，不涉及业务代码或配置行为改动
- Work Log:
  - 更新英文文档 [`README.md`](D:/Android/dual-agent-orchestrator/README.md)
    - 明确系统仍以 `planner + executor` 为默认双角色入口，但已支持兼容式多模型扩展
    - 新增 “Multi-model configuration” 段落，说明如何在保留旧配置的同时，引入 `models` 与 `model_routing`
    - 新增 “Health checks and candidate admission” 段落，说明 executor 候选健康探测、健康模型编队、以及全失效时的失败契约
    - 新增 “What each diagnostic surface shows” 段落，区分 `npm run doctor`、`GET /health`、`GET /v1/models` 的职责边界
    - 补充 `doctor` 输出中的 `executor candidate queue summary`，以及多模型模式下的接口使用说明
    - 在 “Current Limitations” 中补充当前只对 executor 候选池做健康筛选、`doctor` 不做实时逐模型探测
  - 更新中文文档 [`Readme-CN.md`](D:/Android/dual-agent-orchestrator/Readme-CN.md)
    - 同步补齐多模型启用方式、兼容规则、健康探测行为与三类诊断入口的区别
    - 同步补充全失效时的行为说明：`NoHealthyExecutorError`、`environment_failure`、`healthy_executor_ids`、`executor_health_results`、同步 `/v1/jobs` 返回 `500`
    - 同步补充当前限制：planner 候选池尚未接入同级别准入逻辑，实时 executor 池状态应以 `/health` 为准
- Remaining Risks:
  - 文档中的多模型示例使用的是说明性地址与模型名，真实环境仍需用户根据自己的 gateway / provider 调整
  - 目前文档已经明确 `/health` 是实时健康入口，但还没有把该入口补成可完全无依赖注入的集成测试；这属于后续 Task 9 / Final check 的验证关注点
- Next Step:
  - 执行 Task 9：做最终全面 review、修缮与收尾，确认无已知高风险问题并为最终 Large Check C / Final 做准备

### Task 9
- Status: completed
- Objective: 做最终全面 review、修缮与收尾，确认无已知高风险问题并标记完成。
- Verification:
  - 已通过类型检查：`npm run typecheck`
  - 已通过 team 路由回归验证：`node --import tsx --test test/integration/team.run-team.test.ts`
  - 已通过多模型配置与健康筛选单测：`node --import tsx --test test/unit/config.test.ts`
  - 已复核关键实现文件与文档：`src/config.ts`、`src/model-health.ts`、`src/index.ts`、`src/team.ts`、`README.md`、`Readme-CN.md`
- Work Log:
  - 对多模型改造做了收尾级代码审查，重点复查了：
    - legacy `planner/executor` 向 `modelRegistry` / `modelRouting` 的兼容归一化
    - executor 候选健康筛选进入 task/team 主链路后的失败契约
    - `doctor`、`/health`、`/v1/models` 三类控制面输出的一致性
    - team mode 下 per-agent 模型覆盖与全局 executor 候选池叠加时的优先级行为
  - 本轮确认并修复了一个真实行为风险：
    - 问题：team mode 中如果全局 `modelRouting.executorCandidates` 把其他模型排在前面，`buildAgentScopedConfig()` 在重新物化运行时配置后，可能让全局候选重新抢占 `executor`，从而削弱 assignee 指定模型的优先级
    - 修复：在 [`src/team.ts`](D:/Android/dual-agent-orchestrator/src/team.ts) 中，agent-scoped executor / planner 覆盖时会同步把对应的 `executorCandidates` 或 `plannerCandidates` 收敛为 `*.default`，确保 agent 覆盖在该次运行上下文中是稳定生效的
    - 回归：在 [`test/integration/team.run-team.test.ts`](D:/Android/dual-agent-orchestrator/test/integration/team.run-team.test.ts) 新增用例，验证“即使全局 executor 候选排序把其他模型放在前面，team agent 指定模型仍然获胜”
  - 结论：当前实现已经满足本 goal 的核心目标
    - 旧双模型配置仍可直接启动
    - 多模型候选池可通过 `models` + `model_routing` 启用
    - 多模型模式下会先做 executor 健康探测，只有健康模型进入执行队伍
    - 全失效时会提前失败，并向控制面与事件流暴露结构化诊断信息
    - 中英文文档已同步到新的行为契约
- Remaining Risks:
  - 当前健康筛选只接入了 executor 候选池，planner 候选池仍未接入同级别健康准入；这与本次用户需求一致，但它仍是一个明确的后续扩展点
  - `npm run doctor` 仍然是同步配置诊断而不是实时逐模型探测入口；实时池状态应以 `/health` 为准，这一点已在文档中说明
  - `test/integration/job-store.api.test.ts` 整文件在本环境中仍会受既有 restart recovery 慢测例影响而超时，因此控制面更深层的整文件级绿灯仍不是稳定证据；当前依赖定点验证与已有聚焦回归作为补强
- Next Step:
  - 执行 Large Check C / Final：做最后最大的全局检查，确认 goal 是否可以最终标记完成

### Large Check C / Final
- Status: completed
- Objective: 最终最大检查，覆盖需求、代码质量、安全、数据一致性、权限、错误处理、测试、构建、文档、回滚与交付质量。
- Verification:
  - 已通过构建：`npm run build`
  - 已通过类型检查：`npm run typecheck`
  - 已通过多模型配置与健康筛选单测：`node --import tsx --test test/unit/config.test.ts`
  - 已通过 OpenAI-compatible API 合同回归：`node --import tsx --test test/integration/api.contract.test.ts`
  - 已通过 team 路由回归：`node --import tsx --test test/integration/team.run-team.test.ts`
  - 已通过控制面定点终检：`npx tsx -`，结果为 `large-check-c-targeted-control-plane: ok`
- Work Log:
  - 需求对齐检查：
    - 已确认本 goal 的核心需求全部落地
    - 旧双模型配置仍然可直接运行
    - 可通过 `models` + `model_routing` 启用多模型候选池
    - 多模型模式下会先对 executor 候选逐个做健康探测
    - 只有健康 executor 会被编入执行队伍
    - 全部 executor 无效时，会在真正执行前失败，并返回结构化诊断信息
  - 代码与行为检查：
    - 多模型标准化、运行时物化、健康探测、失败分类、控制面暴露、team per-agent 覆盖逻辑已形成闭环
    - 已复查 `src/config.ts`、`src/model-health.ts`、`src/index.ts`、`src/team.ts` 的主链行为，没有发现新的高风险一致性问题
    - 最后一轮收尾中修复的 team agent 覆盖优先级问题已通过回归测试验证
  - 安全与权限检查：
    - 本次改动不涉及密钥存储策略变更、不涉及生产支付/认证高风险路径、不涉及 destructive 文件操作
    - 健康探测使用最小化请求，只读取配置中的模型 endpoint，不修改外部系统状态
  - 数据一致性与错误处理检查：
    - `/health`、`doctor`、`/v1/models` 的职责边界已在实现与文档中对齐
    - `NoHealthyExecutorError`、`environment_failure`、`healthy_executor_ids`、`executor_health_results` 已在 task/team 控制面统一对齐
    - 同步 `/v1/jobs` 在全失效场景下的 `500 + server_error` 契约已被保留并写入测试与文档
  - 文档与回滚检查：
    - 中英文 README 已同步多模型配置、健康探测行为、失败契约与限制说明
    - 本次改造保持为兼容扩展，回滚路径清晰：不使用 `models` / `model_routing` 时系统仍按 legacy `planner/executor` 运行
- Remaining Risks:
  - 当前已知但可接受的边界只剩两类：
    - 健康筛选目前只覆盖 executor 候选池，planner 候选池尚未接入同级别健康准入
    - `test/integration/job-store.api.test.ts` 整文件在本环境里仍会受既有 restart recovery 慢测例影响而超时，因此控制面证据主要依赖聚焦回归与定点脚本化核验
  - 以上两点都已在 `tasks.md` 与文档中明确，不构成本 goal 的已知高风险阻塞
- Next Step:
  - Goal 1 完成
