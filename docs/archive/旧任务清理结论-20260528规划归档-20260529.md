# 【已完成】旧任务清理结论：20260528 规划归档

- 日期：2026-05-29
- 范围：清理 `docs/` 下 `20260528` 的清单、规划、任务类文档，确认哪些事项已经完成，哪些不应再作为当前任务继续派生
- 结论用途：
  - 作为后续新规划前的统一清理入口
  - 明确旧文档的“归档/保留/迁移”状态

## 1. 本次清理覆盖的文档

- `docs/落实度清单-任务分拣与四类执行模式-20260528.md`
- `docs/路线图日志-GoalMode与任务分拣设计-20260528.md`
- `docs/设计目标-Skill感知Planner与技能安装应用方案-20260528.md`

## 2. 总体判断

这 3 份文档都不应再作为“当前主任务清单”直接推进。

原因不是它们失效，而是其中大部分基础任务已经被代码吸收，继续沿着旧清单逐条派生，会把“已完成的基础建设”和“当前真正未完成的主线”混在一起。

当前更合理的管理方式是：

- 将 `20260528` 文档视为历史设计与阶段性快照
- 将仍有效的后续工作收束到新的总规划入口
- 新规划只围绕当前尚未完成的主线展开

## 3. 已完成或基本完成的事项

以下内容已不适合继续挂在旧清单里反复追踪：

- 顶层任务分拣
  - `src/intent-router.ts`
  - `src/intent-dispatch.ts`
- `direct_answer` 执行模式
  - `src/direct-answer.ts`
- `research` 显式 runtime
  - `src/research-runtime.ts`
- `coding` 显式 runtime
  - `src/coding-runtime.ts`
  - 已具备 `understand -> edit -> verify -> summarize` 分阶段执行
- route / workflow / verification / timeline / dashboard 基线
- skill 基础链路
  - `src/skill-registry.ts`
  - `src/skill-installer.ts`
  - `src/skill-runtime.ts`
  - builtin skill manifests
  - `candidateSkills / selectedSkill`
  - skill install observability

这意味着：

- `20260528` 文档中关于“新增 intent router / direct-answer / research-runtime / coding-runtime / skill 基础层”的任务，原则上都应视为已完成或已被后续实现替代
- 后续不应再从这些条目继续拆新的基础任务

## 4. 仍未完成、但应迁移到新规划入口的事项

### 4.1 Goal Mode 主线

这条线仍然有效，但不应继续挂在 `20260528` 的混合型旧文档里。

应迁移并集中到：

- `docs/总规划入口-GoalMode-20260529.md`

当前仍值得继续推进的核心项包括：

- `goal` durable runtime 的完整闭环
- `/v1/goals` 后续完善
- `run-next`
- `large_check`
- `final_review`
- goal 持久化真源与 markdown 镜像协同

### 4.2 Skill 自进化主线

skill 基础设施已基本具备，后续重点已不是“要不要做 skill 层”，而是“如何让 skill 在真实使用中可审计地持续改进”。

应迁移并集中到：

- `docs/总规划入口-Skill自进化-20260529.md`
- `docs/集成设计草案-Skill自进化接入本仓库-20260529.md`
- `docs/执行清单-Skill自进化集成任务拆解-20260529.md`

当前仍值得继续推进的核心项包括：

- `skill-outcome`
- `skill-reflection`
- proposal / auditor / validation
- 自进化链路的存储、事件、验证与准入

## 5. 对 20260528 三份文档的处理结论

### A. 落实度清单-任务分拣与四类执行模式-20260528

处理结论：

- 保留
- 归类为历史落实度快照
- 不再作为当前任务清单直接执行

保留价值：

- 可回看当时对 `direct_answer / research / coding / goal` 的完成度判断
- 可用于解释为什么后续规划改为只聚焦 `goal mode`

### B. 路线图日志-GoalMode与任务分拣设计-20260528

处理结论：

- 保留
- 拆分理解

其中：

- “任务分拣 / direct_answer / research / coding 基线”部分视为已完成并归档
- “goal mode 设计”部分仍保留参考价值，但后续执行入口改为 `20260529` 的 GoalMode 总规划

### C. 设计目标-Skill感知Planner与技能安装应用方案-20260528

处理结论：

- 保留
- 归类为 skill 基础层历史设计稿
- 不再作为当前 skill 主任务入口

保留价值：

- 可回看 skill 基础层最初设计目标
- 可解释为什么后续重心从“基础 skill 接入”转向“skill 自进化”

## 6. 清理后的当前有效入口

如果接下来要继续规划，不再从 `20260528` 旧文档起步，而应使用：

- Goal Mode：
  - `docs/总规划入口-GoalMode-20260529.md`
- Skill 自进化：
  - `docs/总规划入口-Skill自进化-20260529.md`
  - `docs/集成设计草案-Skill自进化接入本仓库-20260529.md`
  - `docs/执行清单-Skill自进化集成任务拆解-20260529.md`

## 7. 最终结论

`docs/` 下 `20260528` 的清单、规划、任务文档，本轮已经完成清理判断：

- 已完成的基础项，不再继续作为现行任务推进
- 仍有效但未完成的事项，已经有 `20260529` 的新入口承接
- 后续新规划应建立在“旧清单已归档”的前提上，而不是继续从旧文档增量改写
