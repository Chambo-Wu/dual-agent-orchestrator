# 【部分完成】当前执行清单：未完成主线

- 日期：2026-05-29
- 用途：作为后续继续推进时的唯一主执行清单
- 范围：只保留当前仍未完成、且值得继续推进的主线

## 1. 清理后的结论

以下内容不再进入当前执行清单：

- `intent router`
- `intent-dispatch`
- `direct-answer`
- `research-runtime`
- `coding-runtime`
- skill 基础注册 / 安装 / runtime / builtin manifests
- route / workflow / verification / timeline / dashboard 基线

这些内容已经落地，或已不适合继续作为当前主任务拆解。

当前只保留两条需要继续收口或深化的主线：

1. `GoalMode v1` 收口补强
2. `Skill 自进化` 后续推进

## 2. 当前主线排序

推荐顺序：

1. 先完成 `GoalMode v1` 的契约、测试、文档收口
2. 再持续推进 `Skill 自进化` 后续阶段

原因：

- `GoalMode v1` 主链已经落地，当前剩余风险集中在 `large_check` 语义、API contract、观测 contract 与 final review 策略说明
- `Skill 自进化` 已有基础链路，但仍需要按阶段推进更深的治理与自动化能力

## 3. 主线 A：GoalMode v1

当前判断：`GoalMode v1` 已经不是从零实现阶段。A1-A4/A6 主链已完成，A5 需要拆成两个收口项：`large_check` 语义补强，以及 final review 当前显式 `/review` 策略的文档与测试对齐。

### A1. Goal durable object

- 状态：done
- 当前事实：已具备 durable goal object 与持久化真源
- 核心项：
  - `src/goal-types.ts`
  - `src/goal-store.ts`
  - `runtime/goals/<goalId>/goal.json`
  - `input.md`
  - `plan.md`
  - `tasks.md`
- 已完成验收：
  - 能创建 goal 真源
  - 能同步 markdown 镜像
  - 不影响现有 job/workflow 主链

### A2. Goal create/read API

- 状态：done
- 当前事实：最小 Goal 控制面已落地
- 核心接口：
  - `POST /v1/goals`
  - `GET /v1/goals`
  - `GET /v1/goals/:id`
  - `GET /v1/goals/data`
- 已完成验收：
  - 可创建、列出、读取 goal
  - 响应结构稳定
  - 主生命周期已有 integration / contract test 覆盖

### A3. Run-next supervisor

- 状态：done
- 当前事实：`run-next` 已能每次推进一个 pending task，并复用现有执行链路
- 核心接口：
  - `POST /v1/goals/:id/run-next`
- 核心行为：
  - 读取 goal
  - 找到下一个未完成 task
  - 创建并执行单次 job
  - 回写 task 状态、job id、output summary、verification summary、run history
- 已完成验收：
  - 一次只推进一个 task
  - 失败、阻塞、完成状态清晰
  - 可重复调用推进

### A4. Retry / Resume / Review

- 状态：done
- 当前事实：goal 执行控制闭环已具备
- 核心接口：
  - `POST /v1/goals/:id/retry`
  - `POST /v1/goals/:id/resume`
  - `POST /v1/goals/:id/review`
- 已完成验收：
  - blocked goal 可恢复
  - 当前 task 可重试
  - review 行为可单独触发

### A5-1. Large check 语义补强

- 状态：done
- 当前事实：自动规划任务时已有 `large_check` 插入能力；显式传入 `tasks` 时默认不隐式改写，但可通过 `insert_large_checks: true` 显式请求插入
- 目标：明确并实现显式 `tasks` 场景下的 `large_check` 策略
- 当前策略：
  - 自动规划任务：继续每 3 个普通 task 插入 `large_check`
  - 显式传入 tasks：默认不隐式改写用户任务列表；如需要自动插入，传 `insert_large_checks: true`
- 已完成验收：
  - 行为有 unit 或 integration/API contract 覆盖
  - docs 明确自动规划与显式 tasks 的差异
  - 新读者不会误以为所有 create goal 场景都会无条件插入 `large_check`

### A5-2. Final review 显式 `/review` 策略

- 状态：done
- 当前事实：final review 当前通过 `POST /v1/goals/:id/review` 显式触发；不是 `run-next` 完成所有 task 后自动触发
- 目标：把 v1 策略明确为显式 `/review`，并让文档与测试命名保持一致
- 当前策略：
  - v1 保持显式 `/review` 触发，避免 `run-next` 自动产生额外 job
  - 后续如需要自动触发，单独增加配置开关或新任务
- 已完成验收：
  - docs 与 API 行为一致
  - 测试命名不暗示自动 final review
  - final review 结果仍有持久化记录

### A6. Goal observability

- 状态：done
- 当前事实：goal events、timeline、dashboard、health summary 基础能力已落地
- 核心项：
  - goal 相关事件
  - goal 与 job 的关联展示
  - 最小 health / API 可见性
  - `/v1/goals/dashboard`
  - `/v1/goals/:id/events`
  - `/v1/goals/:id/timeline`
- 已完成验收：
  - timeline 可看到 goal 推进过程
  - dashboard/API 可读取 goal 当前状态
- 已完成补强：
  - 为 `/v1/goals/data`、`/v1/goals/dashboard`、`/v1/goals/:id/events`、`/v1/goals/:id/timeline` 增加更系统的 API contract 覆盖

## 4. 主线 B：Skill 自进化

### B1. Outcome + Reflection

- 状态：done
- 目标：先把 skill 使用结果和归因链路做完整
- 核心项：
  - `src/skill-evolution-types.ts`
  - `src/skill-outcome.ts`
  - `src/skill-reflection.ts`
  - reflection events / API / timeline
- 核心能力：
  - `SkillOutcomeSummary`
  - `SkillReflectionRecord`
  - `discovery`
  - `optimization`
  - `skill_defect`
  - `execution_lapse`
- 验收：
  - 有 selected skill 时可稳定产出 outcome/reflection
  - timeline / API 可读
  - 不影响无 skill 场景

### B2. Proposal store + proposal generation

- 状态：done
- 目标：把“可改进 skill”的想法沉淀为可审计 proposal，而不是直接改 `skills/`
- 核心项：
  - `src/skill-evolution-store.ts`
  - proposal records
  - candidate dir
  - proposal API
- 验收：
  - proposal 可创建、读取、列出、更新状态
  - proposal 落在 `runtime/skill-evolution/`
  - 不直接污染 builtin skills

### B3. Auditor gate

- 状态：done
- 目标：在 proposal 应用前做 deterministic 审核
- 核心项：
  - schema 校验
  - patch scope 校验
  - tool/runtime/install source 不升级
  - 无 secret / task 泄漏
- 验收：
  - 高风险 patch 被拦截
  - proposal 状态机清晰
  - 单测覆盖关键拒绝场景

### B4. Deployment validation

- 状态：done
- 目标：让 candidate skill 与 baseline 做真实对比，而不是只看 proposal 文本
- 核心项：
  - baseline run
  - candidate run
  - validation report
- 验收：
  - candidate 未改进或变差时失败
  - candidate 确实改善时通过

### B5. Limited automation

- 状态：done
- 目标：仅对白名单低风险 skill 开启有限自动化
- 建议白名单：
  - `find.official_sources`
  - `find.workspace_files`
- 验收：
  - 非白名单 skill 不自动 accept
  - 自动化开关可控、默认保守

## 5. 推荐实施顺序

### 第一阶段：GoalMode v1 收口

状态：done

1. A5-1 `large_check` 语义补强
2. A6 观测 endpoint contract 补强
3. A5-2 final review 显式 `/review` 策略对齐

目标：

- 让 `GoalMode v1` 的行为、测试、文档完全闭环

### 第二阶段：Skill 自进化 v2 深化

状态：next

1. Proposal Generator v2
2. Deployment Validation v2
3. Observability / Ops View v2
4. SKILL.md 双结构治理
5. Auditor Gate v2
6. Automation Risk Tiering v2

目标：

- 在 v1 主链已落地的基础上，推进高质量 rewrite、真实 candidate runtime replay、运营视图、全量 SKILL.md 治理和动态风险自动化

## 6. 本轮建议只承诺到哪里

如果要控制范围，当前第一轮已完成：

1. `GoalMode v1` contract/test/docs 收口
2. `SKILL.md` inventory / check 设计

已经做到：

- `goal` 可创建、读取、推进、重试、恢复、review，且文档不再把这些主链误标为 todo
- `large_check` 在自动规划与显式 tasks 场景下语义清晰
- final review 明确为显式 `/review` 触发
- goal observability endpoints 有 contract 覆盖
- `SKILL.md` inventory / check 方案已形成

下一轮暂不承诺：

- 完整真实 candidate runtime replay
- 高质量自动 rewrite 一步到位
- 高风险 coding skill 自进化
- 全量 live `SKILL.md` 一次性迁移

## 7. 当前执行入口

后续如果继续推进，建议优先参考：

1. `docs/当前执行清单-未完成主线-20260529.md`
2. `docs/总规划入口-GoalMode-20260529.md`
3. `docs/总规划入口-Skill自进化-20260529.md`
4. `docs/集成设计草案-Skill自进化接入本仓库-20260529.md`
