# 【已完成】里程碑：项目主要功能实现

- 日期：2026-05-30
- 目的：沉淀 README 中的里程碑摘要，给后续路线回看提供按日期排列的实现事实。

## 2026-05-26：Workflow 控制面稳定化

- Job dashboard / timeline 恢复文档完成。
- 可恢复事件流、stream replay、阶段 3 操作清单完成。
- Workflow 观测与恢复语义开始固化为前端可消费契约。

## 2026-05-27：前端恢复状态与 CTA 契约

- 明确 recovery state 与 CTA 行为。
- 明确 retry / resume / cancel 在前端控制面的展示与触发语义。
- 补齐 stream replay 与 workflow 状态契约，降低前端恢复场景的不确定性。

## 2026-05-28：Goal Mode 与 Skill 基础层规划

- Goal Mode 主线从旧任务清单中分离。
- Skill-aware planner / install / workflow 方向完成规划收口。
- 旧规划任务完成清理，保留 Goal Mode 与 Skill 自进化两条当前主线。

## 2026-05-29：Skill 自进化 v1 控制面

- Outcome Capture、Reflection Record、Proposal Generator、Auditor Gate、Deployment Validation、Decision / Rollback 主链打通。
- Proposal queue、accepted history、rollback guide、Ops summary、funnel / aging 首批控制面信号落地。
- SKILL.md 双结构治理完成首批 inventory / check / live skill 迁移。
- Auditor Gate v2 补入 high-risk profile、identity drift、verification contract diff whitelist、markdown section responsibility checks。
- Automation Risk Tiering v2 补入 dynamic risk、automation ceiling、cooldown、eligibility 与 stuck-state 首批信号。

## 2026-05-30：Runtime Replay Validation checkpoint

- Deployment Validation 从 heuristic compare 推进到 deterministic isolated manifest replay。
- Validation report 增加 replay job / task contract、step summary、replay-local event stream。
- Candidate runtime 边界从 `config_prepared` 推进到 `workflow_materialized`。
- 新增 deterministic candidate workflow execution harness，通过真实 workflow runtime 执行 materialized candidate workflow。
- Manual validate endpoint 接入 runtime replay，report 可进入：
  - `runtimeBoundary.stage = executed`
  - `runtimeBoundary.contract = true_candidate_runtime_replay`
  - `sameInputComparison.readiness = ready`
- 新增 `skill_evolution.runtime_replay_in_auto_pipeline` opt-in 开关。
  - 默认关闭，保持旧自动链路安全边界。
  - 开启后 auto-validate 可消费 runtime replay report。
  - Auto-accept 仍受 `auto_accept`、risk tier、dynamic ceiling、stability、validation readiness 共同约束。

## 2026-06-14：index.ts 拆分收口与 Skill Evolution 边界加固

- `src/index.ts` 从早期约 8700 行逐步拆分到约 140 行，定位收敛为 CLI/export 聚合层与路由入口转发。
- HTTP 路由装配下沉到 `src/server/router.ts`，路由辅助拆为 `auth.ts`、`sse.ts`、`request-flags.ts`、`job-request.ts`、`job-event-query.ts`。
- 执行入口下沉到 `execution-service.ts` / `task-execution.ts`，Team agent 解析下沉到 `team-agents.ts`。
- Job response、workflow summary、workflow event 聚合下沉到 `job-response.ts`；health/workflow payload 下沉到 `server-response.ts`。
- Chat/Tool/Anthropic 消息归一化下沉到 `chat-message-utils.ts`，流式进度聚合下沉到 `progress-updates.ts`，API request shared types 收敛到 `api-types.ts`。
- Skill Evolution 自动 reflect/propose/audit/validate/accept 流水线下沉到 `skill-evolution-automation.ts`，reflection/proposal 构建桥接下沉到 `skill-evolution-builders.ts`。
- 修复并补测 Skill Evolution 路径一致性：live target 限定在 workspace 内，candidate / rollback target 限定在 snapshot root 内，audit / validate / accept / replay 均复用统一 path helper。
- 修复测试配置覆盖副作用：`setConfigOverrideForTests` 改为 clone 后归一化，不再原地修改调用方 config。
- 清理 `skill-replay-runtime.ts` 死代码，移除 `chat-routes.ts` 的请求类型重复定义与消息桥接 `as any`。
- 验证：`npm run build` 通过，`npm run test:unit` 226/226 通过；skill-evolution endpoints/auto、api.contract/job-events 等定向测试通过。

## 当前剩余重点

1. 继续评估 opt-in runtime replay 下的 auto-accept 放大条件与风险保护。
2. 继续深化 Proposal Generator v2 的高质量结构化 rewrite。
3. 继续推进独立 Ops 页面体验与更细 stuck-state 分类。
4. 继续保持 index.ts 作为聚合层，新增能力优先进入专门模块。
