# 【已完成】今日行动路线：Goal 任务收口

- 日期：2026-05-30
- 总目标：把当前路线中已经进入可收口状态的治理能力闭环，形成“可执行、可观测、可解释、文档一致”的阶段完成状态。
- 执行原则：分阶段推进，每阶段完成代码/测试/文档验收后进入下一阶段；在本轮总目标完成前不主动中止。

## Phase A：路线固化

- 状态：done
- 目标：把今天要完成的范围从长期路线中切出来，避免把完整真实 runtime replay 这种大工程混入今日收口。
- 任务：
  - 明确今日总目标聚焦 v2 治理收口。
  - 保留真实 candidate runtime replay 为后续独立主线。
- 验收：
  - 本文档存在并可作为今日执行入口。

## Phase B：Dynamic Risk 冷却窗口

- 状态：done
- 目标：让 dynamic ceiling 不只看失败数量，也能解释失败信号在多长窗口内有效。
- 任务：
  - 为 dynamic risk 输出 `window_hours / newest_signal_at / cooldown_until / cooldown_active`。
  - recent audit / validation / replay signal 在窗口内触发 ceiling 收紧。
  - Ops / proposal detail 能消费冷却原因。
- 验收：
  - 有 integration 覆盖冷却窗口与 early-stage ceiling。
  - `npm run typecheck` 通过。

## Phase C：Ops 可解释性增强

- 状态：done
- 目标：让操作人能看到 proposal 被动态风险挡住的具体原因和冷却状态。
- 任务：
  - `ops_summary` 暴露 cooldown 字段。
  - jobs dashboard 摘要显示 cooldown 状态。
- 验收：
  - dashboard unit test 覆盖显示文案。
  - observability API test 覆盖字段。

## Phase D：文档状态同步

- 状态：done
- 目标：把 `Goal任务-待完成路线分步实施计划` 与当前实现事实同步。
- 任务：
  - 更新 S4-1 / S4-2 的完成事实。
  - 标记 remaining work：真实 runtime replay、独立 Ops 页面、语义级 auditor 深化。
- 验收：
  - docs 中不再把今日已完成事项列为待办。

## Phase E：最终验证

- 状态：done
- 目标：确认今日改动没有破坏既有主链。
- 验收命令：
  - `node --import tsx --test test/integration/observability.api.test.ts`
  - `node --import tsx --test test/unit/dashboard-ui.test.ts`
  - `node scripts/check-skill-markdown-structure.mjs --warn-only`
  - `npm run typecheck`
